# HEARTBEAT.md

# Periodic checks:
- [ ] Verify mindmap.html reflects current state (skills, contacts, groups, models). Update if needed.
- [ ] If mindmap.html was updated, copy to index.html and push to GitHub (git add index.html && git commit -m "update mindmap" && git push).
